## 概要

- `void multiple_zeta_transform(vector<T>& a)` :  
    次の条件を満たす $b$ を計算し、 $a$ に代入する。
    - $\displaystyle b_i = \sum_{i \mid j} a_j$
    
- `void multiple_moebius_transform(vector<T>& a)` :  
    次の条件を満たす $b$ を計算し、 $a$ に代入する。
    - $\displaystyle a_i = \sum_{i \mid j} b_j$
