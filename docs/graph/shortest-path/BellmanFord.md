## 概要

負辺のあるグラフに対して単一始点最短経路問題を解く。負閉路の検出もできる。

- `vector<T> BellmanFord(int N, Edges<T> E, int s = 0)` :  
辺のリスト `E` で表される頂点数 `N` のグラフに対して、各頂点の `s` からの距離を求める。  
`INF` の場合 `s` から到達不可能、 `-INF` の場合 `s` から到達可能な負閉路から到達可能。  
$\Theta(V \times E)$ 。
