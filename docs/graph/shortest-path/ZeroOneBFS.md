## 概要

BFS の進化版。 deque を使うことで、辺の長さが 0 または 1 であるときに線形時間で最短経路問題を解ける。 Dijkstra よりもオーダーが良い。

- `vector<T> ZeroOneBFS(Graph<T> G, int s = 0)` : 各辺の長さが $0$ または $1$ であるグラフ `G` において、各頂点の `s` からの距離を求める。 $\Theta(V+E)$ 。
